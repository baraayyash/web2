{
    "name": "express-mongo-crud",
    "version": "1.0.0",
    "description": "Express + MongoDB CRUD examples (standalone and related models)",
    "main": "src/server.js",
    "scripts": {
      "start": "node src/server.js",
      "dev": "node --watch src/server.js",
      "docker:up": "docker compose up -d",
      "docker:down": "docker compose down"
    },
    "engines": {
      "node": ">=18"
    },
    "dependencies": {
      "dotenv": "^16.4.5",
      "express": "^4.21.0",
      "mongoose": "^8.8.0"
    }
  }
  